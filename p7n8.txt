# pb1.yml

- name: My first Playbook
  hosts: local
  connection: local
  tasks:

    - name: Writing my first task
      debug:
        msg: "Ansible is a popular config mgmt tool"

# hello_world.py

#!/usr/bin/python

from ansible.module_utils.basic import AnsibleModule

def run_module():

    module_args = dict(
        name=dict(type='str', required=True)
    )

    module = AnsibleModule(argument_spec=module_args)

    result = {
        "changed": False,
        "message": f"Hello, {module.params['name']}!"
    }

    module.exit_json(**result)

if __name__ == '__main__':
    run_module()

# pb2.yml

---
- name: Ansible with 1 module
  hosts: localhost
  gather_facts: no

  tasks:

    - name: Module Command
      hello_world:
        name: "Ansible"
      register: result

    - name: Show message
      debug:
        msg: "{{result.message}}"
# p8 pipeline
pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Anindita-23/devops-practise.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }

        stage('Archive Artifacts') {
            steps {
                archiveArtifacts artifacts: '**/target/*.jar',
                                  allowEmptyArchive: true
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                export ANSIBLE_HOST_KEY_CHECKING=False
                ansible-playbook -i hosts.ini mydeploy.yml \
                --extra-vars="ansible_become_pass=exam@cse"
                '''
            }
        }

    }
}

# mydeploy.yml

---
- name: Deploy Mave Artifact
  hosts: local
  become: true
  become_user: student
  become_method: su

  tasks:

    - name: Copy the artifact to the deployment library
      copy:
        src: "/var/lib/jenkins/workspace/pgm8/target/MavenProject-1.0-SNAPSHOT.jar"
        dest: "/home/student/cs098/p8.jar"
